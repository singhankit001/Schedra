# hooks/

Reserved for shared, cross-feature React hooks (e.g. media-query or
outside-click helpers) once a concrete need exists.

Deliberately empty in this phase to avoid speculative abstractions —
add hooks here as real usages arise.
