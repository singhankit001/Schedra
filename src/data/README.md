# data/

Typed static/reference data — seed fixtures, constant lookup tables.

- `mock-account.ts` — placeholder sidebar-footer/header account (Phase 3).
  No authentication exists yet; this is local, typed, non-personal data,
  not a mock API response.
- `mock-notifications.ts` — placeholder header notification list (Phase 4).

Still no fabricated Google data or fake API response fixtures here —
those stay out until a real integration exists.
