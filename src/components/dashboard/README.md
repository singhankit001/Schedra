# components/dashboard/

Dashboard-page-specific components, documented in `visual-spec/components.md`.

Implemented (Phase 4): `dashboard-header.tsx`, `dashboard-search.tsx`,
`notification-button.tsx`, `welcome-banner.tsx`, `welcome-illustration.tsx`.

Still not built: stat cards, meeting list rows, the mini calendar,
meeting-type cards, the New Meeting CTA — later phases.
